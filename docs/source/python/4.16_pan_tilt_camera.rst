.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_pan_tilt_camera:

4.16 云台摄像头控制系统
========================

**简介**

在本项目中，您将创建一个可以使用摇杆进行水平（平移）和垂直（倾斜）运动的摄像头控制系统。该系统允许您远程控制安装在舵机上的摄像头方向，实时预览摄像头画面，并通过按下摇杆按钮拍摄照片。该项目非常适合监控应用、摄影项目或学习舵机控制和摄像头集成。

----------------------------------------------

**所需材料**

本项目需要以下组件：

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - 组件介绍
        - 购买链接

    *   - :ref:`cpn_breadboard`
        - |link_breadboard_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_servo`
        - |link_servo_buy|
    *   - :ref:`cpn_joystick`
        - \-
    *   - :ref:`cpn_camera_module`
        - |link_camera_buy|
    *   - :ref:`cpn_fusion_hat`
        - \-
    *   - 树莓派
        - \-


--------------------

**电路图**

.. image:: img/fzz/2.1.9_sch.png
   :width: 80%
   :align: center

---------------

**接线图**

#. 为方便使用摄像头模块，建议先\ :ref:`assemble_fusion_hat_pan_tilt`\ 。

   .. note::

     安装云台可能会遮挡部分引脚，因此建议仅在使用摄像头时安装，或在安装后将其置于外侧。


   .. image:: ../quick_start/img/gimbal_assemble.png

#. 按照以下接线图搭建电路：

   .. image:: img/fzz/4.16_joystick_camera_bb.png
      :width: 100%
      :align: center

----------------------------------------------

**运行示例**


1. 访问树莓派桌面：

   * :ref:`remote_desktop`：使用\ **VNC**\ 获得完整的桌面体验。
   * |link_rpi_connect|：使用\ **Raspberry Pi Connect**\ 从任何浏览器安全地访问树莓派。

2. 打开终端并进入代码文件夹：

   .. raw:: html

        <run></run>

   .. code-block:: shell

       cd ~/ai-lab-kit/python

3. 运行脚本启动摄像头：

   .. raw:: html

        <run></run>

   .. code-block:: shell

       sudo python3 pan_tilt_camera.py

4. 运行脚本后，云台摄像头系统启动并初始化摄像头和舵机。

   * 如果有显示器可用，则显示实时摄像头预览；否则，程序在无头模式下正常运行。

   * 向左或向右移动摇杆可使摄像头水平旋转（平移），向上或向下移动则可垂直倾斜（倾斜）。

   * 按下摇杆按钮时，摄像头拍摄一张照片并保存到 ``Pictures/camera_pan_tilt`` 目录，使用简单的连续文件名如 ``photo_001.jpg``。

   * 系统持续运行并响应用户输入，直到您按 Ctrl + C 停止程序。

----------------------------------------------

**代码**

以下是本项目使用的 Python 脚本：

.. raw:: html

   <run></run>

.. code-block:: python

    #!/usr/bin/env python3
    import os, time
    from picamera2 import Picamera2, Preview
    from fusion_hat.adc import ADC
    from fusion_hat.pin import Pin, Mode, Pull
    from fusion_hat.servo import Servo

    # Servo channels for pan (horizontal) and tilt (vertical)
    PAN_CHANNEL, TILT_CHANNEL = 2, 3

    # Joystick ADC pins (X/Y axis) and button pin
    X_PIN, Y_PIN = "A1", "A0"
    BTN_PIN = 17

    # Angle limits to protect servos
    PAN_MIN, PAN_MAX = -90, 90
    TILT_MIN, TILT_MAX = -45, 45

    # Deadzone ignores small joystick movement
    DEADZONE = 15
    MOVE_SPEED = 3
    LOOP_DELAY = 0.05

    # Photo save directory (works with sudo)
    REAL_USER = os.getenv("SUDO_USER") or os.getlogin()
    PHOTO_DIR = os.path.join(f"/home/{REAL_USER}", "Pictures", "camera_pan_tilt")
    os.makedirs(PHOTO_DIR, exist_ok=True)

    # Initialize servos
    pan_servo = Servo(PAN_CHANNEL)
    tilt_servo = Servo(TILT_CHANNEL)

    # Initialize joystick and button (active-low)
    x_adc = ADC(X_PIN)
    y_adc = ADC(Y_PIN)
    joystick_button = Pin(BTN_PIN, mode=Mode.IN, pull=Pull.UP)  # pressed -> 0

    # Initialize camera
    camera = Picamera2()
    camera.configure(camera.create_preview_configuration(main={"size": (1280, 720)}))

    preview_started = False
    photo_count = 1
    current_pan = 0
    current_tilt = 0
    last_button_state = 1  # Used for edge detection

    def clamp(v, vmin, vmax):
        # Limit value to a safe range
        return max(vmin, min(vmax, v))

    def map_value(value, in_min, in_max, out_min, out_max):
        # Map ADC value to a new range
        return (value - in_min) * (out_max - out_min) / (in_max - in_min) + out_min

    def apply_deadzone(v, dz):
        # Ignore small joystick movement
        return 0 if (-dz < v < dz) else v

    def read_joystick():
        # Read joystick X/Y position
        x = map_value(x_adc.read(), 0, 4095, -100, 100)
        y = map_value(y_adc.read(), 0, 4095, -100, 100)
        return x, y

    def check_button_press():
        # Detect button press (HIGH -> LOW)
        global last_button_state
        current_state = joystick_button.value()
        if last_button_state == 1 and current_state == 0:
            last_button_state = current_state
            return True
        last_button_state = current_state
        return False

    def take_photo():
        # Capture and save one photo
        global photo_count
        filename = f"photo_{photo_count:03d}.jpg"
        filepath = os.path.join(PHOTO_DIR, filename)
        camera.capture_file(filepath)
        print("Saved:", filepath)
        photo_count += 1

    def start_preview_if_available():
        # Start camera preview only if a display is available
        global preview_started
        preview_started = False
        if os.getenv("DISPLAY"):
            try:
                camera.start_preview(Preview.QT)
                preview_started = True
            except Exception:
                preview_started = False

    def cleanup():
        # Safely stop camera and release resources
        try:
            camera.stop()
        except Exception:
            pass
        if preview_started:
            try:
                camera.stop_preview()
            except Exception:
                pass
        try:
            camera.close()
        except Exception:
            pass

    def main():
        global current_pan, current_tilt

        start_preview_if_available()
        camera.start()

        # Center camera at startup
        pan_servo.angle(0)
        tilt_servo.angle(0)

        try:
            while True:
                # Read joystick and move camera
                x, y = read_joystick()
                x = apply_deadzone(x, DEADZONE)
                y = apply_deadzone(y, DEADZONE)

                new_pan = current_pan + (MOVE_SPEED if x > DEADZONE else -MOVE_SPEED if x < -DEADZONE else 0)
                new_tilt = current_tilt + (MOVE_SPEED if y > DEADZONE else -MOVE_SPEED if y < -DEADZONE else 0)

                new_pan = clamp(new_pan, PAN_MIN, PAN_MAX)
                new_tilt = clamp(new_tilt, TILT_MIN, TILT_MAX)

                if new_pan != current_pan:
                    current_pan = new_pan
                    pan_servo.angle(current_pan)

                if new_tilt != current_tilt:
                    current_tilt = new_tilt
                    tilt_servo.angle(current_tilt)

                # Take photo when button is pressed
                if check_button_press():
                    take_photo()

                time.sleep(LOOP_DELAY)

        except KeyboardInterrupt:
            pass
        finally:
            cleanup()

    if __name__ == "__main__":
        main()



----------------------------------------------

**理解代码**

#. **硬件初始化**

   - 初始化两个舵机，分别控制摄像头的水平（平移）和垂直（倾斜）运动
   - 摇杆使用 ADC 通道读取 X 和 Y 模拟值，并使用 GPIO 引脚检测按钮按下
   - 摄像头模块初始化为预览模式，支持有显示器和无头操作

#. **摇杆读取和处理**

   - ``read_joystick()`` 从摇杆的 X 和 Y 轴读取原始模拟值
   - ``map_value()`` 将 ADC 值（0-4095）转换为可用范围 −100 到 100
   - ``apply_deadzone()`` 过滤掉摇杆的小幅移动，防止摄像头不必要的漂移

#. **摄像头运动控制**

   - 摇杆输入被转换为平移和倾斜角度的增量变化
   - ``clamp()`` 确保角度保持在安全范围内，保护舵机
   - 仅当角度发生变化时才更新舵机，提供平滑稳定的运动

#. **按钮按下检测**

   - 摇杆按钮配置为使用上拉电阻的低电平有效输入
   - ``check_button_press()`` 使用边沿检测（HIGH → LOW）检测按钮按下
   - 这确保即使按住按钮，每次按下也只拍摄一张照片

#. **照片拍摄和存储**

   - ``take_photo()`` 使用摄像头模块捕获图像
   - 照片使用连续文件名保存（例如 ``photo_001.jpg``）
   - 所有图像存储在用户的 ``Pictures/camera_pan_tilt`` 目录中

#. **摄像头预览处理**

   - 仅当图形显示可用时才启动实时摄像头预览
   - 脚本在没有显示器的情况下继续正常运行

#. **主循环和清理**

   - 主循环持续读取摇杆输入并实时响应
   - 使用 ``Ctrl + C`` 退出程序时，摄像头安全停止
   - 所有硬件资源被正确释放，确保干净关机


----------------------------------------------

**故障排除**

1. **舵机不移动**：

   - **原因**：舵机连接不正确或电源问题
   - **解决方法**：

     - 验证舵机是否连接到正确的通道（2 和 3）
     - 确保 Fusion HAT 正确供电
     - 检查舵机接线是否松动

2. **摄像头预览不显示**：

   - **原因**：摄像头模块未被检测到或配置错误
   - **解决方法**：

     - 确保摄像头线缆牢固连接到 CSI 接口
     - 检查树莓派配置中是否启用了摄像头
     - 验证摄像头模块的兼容性

3. **摇杆不响应**：

   - **原因**：引脚分配错误或 ADC 问题
   - **解决方法**：

     - 验证摇杆连接到 A0、A1 和 GPIO 17
     - 使用简单的打印语句测试 ADC 读数
     - 检查 Fusion HAT ADC 是否正常工作

4. **照片未保存**：

   - **原因**：权限问题或目录问题
   - **解决方法**：

     - 检查用户主目录中是否存在 Pictures 目录
     - 验证照片目录的写入权限
     - 如果权限问题持续存在，尝试使用 sudo 运行

5. **舵机运动不稳定**：

   - **原因**：电源波动或软件时序问题
   - **解决方法**：

     - 确保为 Fusion HAT 提供稳定的电源
     - 调整 ``MOVE_SPEED`` 和延时值
     - 如有需要，在舵机电源线上添加电容

----------------------------------------------

**扩展思路**

1. **视频录制**：添加带开始/停止控制的视频录制功能：

   .. code-block:: python

      def start_recording():
          timestamp = time.strftime("%Y%m%d_%H%M%S")
          video_path = os.path.join(VIDEO_DIR, f"video_{timestamp}.mp4")
          camera.start_recording(video_path)
          print(f"Recording started: {video_path}")

      def stop_recording():
          camera.stop_recording()
          print("Recording stopped")

2. **预设位置**：创建摄像头预设位置以便快速访问：

   .. code-block:: python

      PRESETS = {
          'center': (0, 0),
          'left': (-45, 0),
          'right': (45, 0),
          'up': (0, 30),
          'down': (0, -30)
      }

      def goto_preset(preset_name):
          if preset_name in PRESETS:
              pan, tilt = PRESETS[preset_name]
              pan_servo.angle(pan)
              tilt_servo.angle(tilt)

----------------------------------------------

**总结**

本项目演示了如何使用树莓派、舵机和摄像头模块创建复杂的云台摄像头控制系统。它将硬件控制、实时视频处理和用户输入处理结合为一个有机整体。该项目为更高级的应用（如监控系统、摄影机器人或互动艺术装置）提供了基础。
