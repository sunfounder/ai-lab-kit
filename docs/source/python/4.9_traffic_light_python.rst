.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_fun_traffic_light:

4.9 交通灯模拟
==============

**简介**

本项目演示如何使用 LED 和 4 位数码管模拟真实的交通灯系统。LED 代表红灯、黄灯和绿灯，而数码管则倒计时显示每个灯的剩余时间。它提供了一种在原型设计或教育环境中学习 GPIO 控制、定时和数码管使用的引人入胜的方式。


----------------------------------------------


**所需材料**

本项目需要以下组件：

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - 组件介绍
        - 购买链接

    *   - :ref:`cpn_breadboard`
        - |link_breadboard_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_resistor`
        - |link_resistor_buy|
    *   - :ref:`cpn_led`
        - |link_led_buy|
    *   - :ref:`cpn_4_digit`
        - \-
    *   - :ref:`cpn_74hc595`
        - |link_74hc595_buy|
    *   - :ref:`cpn_fusion_hat`
        - \-
    *   - 树莓派
        - \-



----------------------------------------------

**电路图**

以下是交通灯模拟项目的原理图：

.. image:: img/fzz/4.1.9_sch.png
   :width: 90%
   :align: center



----------------------------------------------


**接线图**

参考接线图组装组件：


.. image:: img/fzz/4.1.9_bb.png
   :width: 80%
   :align: center


----------------------------------------------

**运行示例**


本教程中使用的所有示例代码均可在 ``ai-lab-kit`` 目录中找到。
按照以下步骤运行示例：


.. raw:: html

   <run></run>

.. code-block:: shell

   cd ~/ai-lab-kit/python/
   sudo python3 4.9_TrafficLight.py


----------------------------------------------

**代码**


以下是交通灯模拟的 Python 脚本：


.. raw:: html

   <run></run>

.. code-block:: python

   #!/usr/bin/env python3

   from fusion_hat.pin import Pin, Mode
   import threading

   # Setup GPIO pins for 74HC595 shift register
   SDI = Pin(17,mode=Mode.OUT)   # Serial Data Input
   RCLK = Pin(4,mode=Mode.OUT)  # Register Clock
   SRCLK = Pin(27,mode=Mode.OUT) # Shift Register Clock

   # Define GPIO pins for digit selection on the 7-segment display
   placePin = [Pin(pin,mode=Mode.OUT) for pin in (23, 24, 25, 12)]

   # Segment codes for numbers 0-9 on 7-segment display
   number = (0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90)

   # Setup GPIO pins for traffic light LEDs
   ledPinR = Pin(5,mode=Mode.OUT) # Red LED
   ledPinG = Pin(6,mode=Mode.OUT)  # Green LED
   ledPinY = Pin(13,mode=Mode.OUT)  # Yellow LED

   # Duration settings for traffic lights
   greenLight = 30
   yellowLight = 5
   redLight = 60

   # Traffic light color names
   lightColor = ("Red", "Green", "Yellow")

   # Initialize state variables
   colorState = 0
   counter = 60
   timer1 = None

   def setup():
      """ Initialize the traffic light system and start the timer. """
      global timer1
      timer1 = threading.Timer(1.0, timer)
      timer1.start()

   def clearDisplay():
      """ Clear the 7-segment display. """
      for _ in range(8):
         SDI.high()
         SRCLK.high()
         SRCLK.low()
      RCLK.high()
      RCLK.low()

   def hc595_shift(data):
      """ Shift data to the 74HC595 shift register for digit display. """
      for i in range(8):
         SDI.value(0x80 & (data << i))
         SRCLK.high()
         SRCLK.low()
      RCLK.high()
      RCLK.low()

   def pickDigit(digit):
      """ Select a specific digit to display on the 7-segment display. """
      for pin in placePin:
         pin.low()
      placePin[digit].high()

   def timer():
      """ Handle the timing for traffic light changes. """
      global counter, colorState, timer1
      timer1 = threading.Timer(1.0, timer)
      timer1.start()
      counter -= 1
      if counter == 0:
         counter = [greenLight, yellowLight, redLight][colorState]
         colorState = (colorState + 1) % 3
      print(f"counter : {counter}    color: {lightColor[colorState]}")

   def lightup():
      """ Update the traffic light LED based on the current state. """
      global colorState
      ledPinR.low()
      ledPinG.low()
      ledPinY.low()
      [ledPinR, ledPinG, ledPinY][colorState].high()

   def display():
      """ Display the current counter value on the 7-segment display. """
      global counter

      for i in range(4):
         digit = counter // (10 ** (3 - i)) % 10
         if i == 0 and digit == 0:
               continue
         clearDisplay()
         pickDigit(i)
         hc595_shift(number[digit])

   def loop():
      """ Main loop to continuously update display and lights. """
      while True:
         display()
         lightup()

   def destroy():
      """ Clean up resources when the script is terminated. """
      global timer1
      timer1.cancel()
      ledPinR.low()
      ledPinG.low()
      ledPinY.low()

   try:
      setup()
      loop()
   except KeyboardInterrupt:
      destroy()



----------------------------------------------


**理解代码**

1. **初始化：**

   * 设置移位寄存器、数码管和交通灯 LED 的 GPIO 引脚。
   * 定义灯光时长的常量并初始化状态变量。

2. **数码管函数：**

   * ``clearDisplay``：清除显示器上的所有段。
   * ``hc595_shift``：向移位寄存器发送数据以控制显示段。
   * ``pickDigit``：激活特定数字用于复用显示。

3. **定时器函数：**

   * 处理倒计时和灯状态之间的转换。

4. **交通灯控制：**

   * 根据当前状态更新 LED。

5. **主循环：**

   * 持续更新显示和 LED。

6. **清理：**

   * 确保在终止时正确释放所有资源。


----------------------------------------------


**故障排除**

1. **交通灯不工作**：

   - **原因**：接线错误或 LED 损坏。
   - **解决方法**：

     - 验证 LED 是否正确连接到 GPIO 引脚 5（红）、6（绿）和 13（黄）。
     - 使用简单的 GPIO 控制脚本单独测试每个 LED。

2. **数码管不显示倒计时**：

   - **原因**：接线或移位寄存器配置错误。
   - **解决方法**：

     - 确保移位寄存器的 ``SDI``、``RCLK`` 和 ``SRCLK`` 引脚正确连接到 GPIO 17、4 和 27。
     - 验证数字选择引脚是否与 ``placePin`` 配置匹配。

3. **倒计时卡死**：

   - **原因**：定时器线程未正确重启。
   - **解决方法**：确保 ``timer()`` 函数调用 ``timer1.start()`` 以重启定时器线程。

4. **灯光时长不正确**：

   - **原因**：时长值或状态转换错误。
   - **解决方法**：

     - 验证 ``redLight``、``greenLight`` 和 ``yellowLight`` 的时长。
     - 检查 ``timer()`` 函数逻辑，确保状态转换正确。

----------------------------------------------


**扩展思路**

1. **行人过街**：添加行人过街按钮，临时中断交通灯周期，让行人安全过街。

   .. code-block:: python

      from fusion_hat import Pin
      pedestrian_button = Pin(22, Pin.IN, Pin.PULL_DOWN)

      def handle_pedestrian():
            global colorState, counter
            if colorState == 1:  # If Green Light
               counter = 5  # Shorten green light duration
      pedestrian_button.when_activated = handle_pedestrian

2. **自适应定时**：根据实时交通流量动态调整交通灯的时长。

3. **事件记录**：将交通灯状态变化和时间戳记录到文件以供分析：

   .. code-block:: python

      with open("traffic_log.txt", "a") as log_file:
            log_file.write(f"{time.strftime('%Y-%m-%d %H:%M:%S')} - {lightColor[colorState]} light\n")

----------------------------------------------

**总结**

本项目模拟了一个交通灯系统，提供了 GPIO 控制、移位寄存器和数码管的实践经验。这是一种有趣且富有教育意义的电子技术和编程探索方式，同时复制了现实世界的应用。尝试增强系统，使其更加互动！
