.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_fun_count:

4.6 计数装置
============

**简介**

在本项目中，您将使用 PIR 运动传感器和 4 位数码管构建一个人流计数系统。每当 PIR 传感器检测到运动时，系统就会递增显示的计数。该项目非常适合监控走廊或入口的人流量，并提供 GPIO 编程和显示控制的实践经验。


----------------------------------------------


**所需材料**

本项目需要以下组件：

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - 组件介绍
        - 购买链接

    *   - :ref:`cpn_breadboard`
        - |link_breadboard_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_resistor`
        - |link_resistor_buy|
    *   - :ref:`cpn_pir`
        - |link_pir_buy|
    *   - :ref:`cpn_4_digit`
        - \-
    *   - :ref:`cpn_74hc595`
        - |link_74hc595_buy|
    *   - :ref:`cpn_fusion_hat`
        - \-
    *   - 树莓派
        - \-



----------------------------------------------


**电路图**

以下是本项目的电路图：


.. image:: img/fzz/4.1.4_sch.png
   :width: 100%
   :align: center

----------------------------------------------


**接线图**

按照接线图正确连接组件：


.. image:: img/fzz/4.1.4_bb.png
   :width: 80%
   :align: center


.. note::

   调整 PIR 模块上的两个电位器以获得最佳灵敏度和检测距离。将两者都逆时针旋转到最大设置以获得最佳效果。

.. image:: ../python/img/PIR_TTE.png
   :width: 300
   :align: center

----------------------------------------------

**运行示例**


本教程中使用的所有示例代码均可在 ``ai-lab-kit`` 目录中找到。
按照以下步骤运行示例：


.. raw:: html

   <run></run>

.. code-block:: shell

   cd ~/ai-lab-kit/python/
   sudo python3 4.6_CountingDevice.py

此 Python 脚本集成了 PIR 运动传感器和由 74HC595 移位寄存器驱动的数码管。脚本执行以下操作：

1. **运动检测**：连接到 GPIO 26 的 PIR 运动传感器检测运动。每次运动检测递增一次计数器。

2. **数字显示**：

   - 当前计数值显示在 4 位数码管上。
   - 显示随运动检测动态更新，递增数值。

3. **持续监控**：脚本持续监控运动并实时更新显示。

4. **优雅退出**：按下 ``Ctrl+C`` 时，所有引脚关闭，确保脚本安全停止。



----------------------------------------------


**代码**


以下是本项目使用的 Python 脚本：



.. raw:: html

   <run></run>

.. code-block:: python

   #!/usr/bin/env python3
   from fusion_hat.pin import Pin, Mode, Pull

   # Initialize PIR motion sensor on GPIO 22 (input with pull-down)
   pir = Pin(22, mode=Mode.IN, pull=Pull.DOWN)

   # Define GPIO pins for the 74HC595 shift register
   SDI = Pin(17, mode=Mode.OUT)   # Serial Data Input
   RCLK = Pin(4, mode=Mode.OUT)   # Register Clock (latch)
   SRCLK = Pin(27, mode=Mode.OUT) # Shift Register Clock

   # Define GPIO pins used to select one of the 4 digits on the 7-segment display
   placePin = [Pin(pin, mode=Mode.OUT) for pin in (23, 24, 25, 12)]

   # Segment code table for digits 0–9 (common-cathode 7-segment display)
   number = (0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90)

   # Counter to display the number of detections
   counter = 0

   def clearDisplay():
      """Clear the 7-segment display by shifting out 'all off' bits."""
      for _ in range(8):
         SDI.high()     # Send a high bit
         SRCLK.high()   # Clock it in
         SRCLK.low()
      RCLK.high()        # Latch output
      RCLK.low()

   def hc595_shift(data):
      """Shift a full byte of data into the 74HC595 to control the segments."""
      for i in range(8):
         SDI.value(0x80 & (data << i))  # Output next bit of the data
         SRCLK.high()                   # Pulse shift clock
         SRCLK.low()
      RCLK.high()                        # Latch data to display
      RCLK.low()

   def pickDigit(digit):
      """Enable one specific digit (0–3) on the multiplexed display."""
      for pin in placePin:
         pin.low()                      # Disable all digits
      placePin[digit].high()             # Activate the selected digit

   def display():
      """Display the current 4-digit counter value on the 7-segment display."""
      global counter

      # Display ones place
      clearDisplay()
      pickDigit(3)
      hc595_shift(number[counter % 10])

      # Display tens place
      clearDisplay()
      pickDigit(2)
      hc595_shift(number[counter % 100 // 10])

      # Display hundreds place
      clearDisplay()
      pickDigit(1)
      hc595_shift(number[counter % 1000 // 100])

      # Display thousands place
      clearDisplay()
      pickDigit(0)
      hc595_shift(number[counter % 10000 // 1000])

   def loop():
      """Main loop: continuously update display and detect PIR motion transitions."""
      global counter
      currentState = 0
      lastState = 0

      while True:
         display()                                      # Refresh 7-segment display
         currentState = 1 if pir.value() == 1 else 0    # Read PIR output

         # Detect rising edge: motion detected now, but not in last cycle
         if currentState == 1 and lastState == 0:
               counter += 1                               # Increase the count

         lastState = currentState                        # Save state for next loop

   try:
      loop()                                             # Start main loop
   except KeyboardInterrupt:
      # Clean up GPIO pins when exiting
      SDI.low()
      SRCLK.low()
      RCLK.low()
      pass


----------------------------------------------

**理解代码**

1. **运动检测：**

   PIR 传感器检测运动并发送信号以递增计数器。

2. **显示更新：**

   ``hc595_shift`` 和 ``pickDigit`` 函数协同工作，控制数码管并实时更新计数器。

3. **主循环：**

   ``loop`` 函数持续检查 PIR 传感器，并在检测到运动时更新显示。

4. **中断处理：**

   脚本确保在中断时安全关闭所有 GPIO 引脚，防止意外行为。



----------------------------------------------


**故障排除**

1. **显示不工作**：

   - **原因**：数码管或移位寄存器接线错误。
   - **解决方法**：

     - 验证 GPIO 引脚与移位寄存器的 SDI、SRCLK 和 RCLK 引脚之间的连接。
     - 确保所有 ``placePin`` 连接与对应的数码管引脚匹配。

2. **运动传感器不触发**：

   - **原因**：PIR 传感器接线或环境干扰。
   - **解决方法**：

     - 检查 PIR 传感器到 GPIO 26、电源和 GND 的连接。
     - 如有可用，调整 PIR 传感器上的灵敏度和延时电位器。

3. **计数器不递增**：

   - **原因**：未检测到运动传感器状态变化。
   - **解决方法**：

     - 确保 ``pir.value()`` 值正确更新。
     - 在 ``loop()`` 函数中添加调试打印以确认状态转换。

4. **显示闪烁**：

   - **原因**：显示更新中的延时不足或时序不当。
   - **解决方法**：在 ``display()`` 函数中添加小延时以稳定显示：

       .. code-block:: python

           import time
           time.sleep(0.01)

----------------------------------------------

**扩展思路**

1. **双向计数器**：添加另一个 PIR 传感器以检测相反方向的运动并递减计数器：

   .. code-block:: python

      pir2 = Pin(16, Pin.IN, pull= Pin.PULL_DOWN)
      if pir2.value()==1:
         counter -= 1

2. **基于阈值的警报**：当计数器超过预设值时触发警报（例如 LED 或蜂鸣器）：

   .. code-block:: python

      from fusion_hat import Buzzer
      buzzer = Buzzer(Pin(22))
      if counter > 50:
            buzzer.on()
      else:
            buzzer.off()

3. **数据记录**：将计数器值和时间戳记录到文件以供分析：

   .. code-block:: python

      with open("motion_log.txt", "a") as log_file:
            log_file.write(f"{time.time():.3f}, Counter: {counter}\n")

4. **基于计时器的重置**：在特定不活动时间后重置计数器：

   .. code-block:: python

      last_motion_time = time.time()
      if time.time() - last_motion_time > 300:  # 5 minutes
            counter = 0

5. **基于事件的触发器**：当计数器达到特定值时触发特定操作（例如控制电器）。


----------------------------------------------


**总结**

本项目介绍了使用 PIR 传感器和数码管进行运动检测和显示控制的基础知识。它为更高级的 IoT 和数据可视化项目提供了实用的基础。
