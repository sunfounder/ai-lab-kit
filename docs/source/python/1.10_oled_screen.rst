.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_oled:


1.10 OLED显示屏
================

**介绍**

在本项目中，我们将学习如何通过Raspberry Pi上的I2C通信与基于SSD1306的OLED显示屏进行交互。OLED（有机发光二极管）显示屏以其高对比度、宽视角和低功耗而著称。本项目演示了如何初始化显示屏、绘制基本形状和渲染文本。

----------------------------------------------

**你需要什么**

要完成本项目，你将需要以下组件：

.. list-table::
   :widths: 30 20
   :header-rows: 1

   *  - 组件
      - 购买链接

   *  - :ref:`cpn_oled`
      - \-
   *  - :ref:`cpn_wires`
      - |link_wires_buy|
   *  - :ref:`cpn_fusion_hat`
      - \-
   *  - Raspberry Pi
      - \-

----------------------------------------------

**接线图**

请按照以下步骤搭建电路：

1. 将OLED显示屏的VCC引脚连接到Fusion HAT+的3.3V。
2. 将OLED显示屏的GND引脚连接到Fusion HAT+的GND。
3. 将OLED显示屏的SCL引脚连接到Fusion HAT+的SCL（GPIO 3）。
4. 将OLED显示屏的SDA引脚连接到Fusion HAT+的SDA（GPIO 2）。

.. image:: img/fzz/1.10_oled_bb.png
   :width: 70%
   :align: center

----------------------------------------------

**设置步骤**

#. 安装SSD1306驱动库：

   该库支持使用Python与SSD1306 OLED显示屏通信。

   .. raw:: html

      <run></run>

   .. code-block:: shell

      sudo pip3 install adafruit-circuitpython-ssd1306 --break


#. 所有示例程序都包含在\ ``ai-lab-kit``\ 目录中。按如下方式运行OLED示例：

   .. raw:: html

      <run></run>

   .. code-block:: shell

      cd ~/ai-lab-kit/python/
      sudo python3 1.10_OLED_Display.py


#. 运行示例时，OLED屏幕将显示一个带边框的框，其中居中显示文本\ **"Hello World!"**\ 。

   这确认了显示屏正在通过I2C成功通信，并且可以渲染文本和图形。

----------------------------------------------

**代码**

以下Python代码初始化OLED显示屏并绘制"Hello World!"文本：

.. raw:: html

   <run></run>

.. code-block:: python

   import board
   import digitalio
   from PIL import Image, ImageDraw, ImageFont
   import adafruit_ssd1306

   # Initialize OLED display dimensions
   WIDTH = 128
   HEIGHT = 64

   # Set up I2C communication with the OLED display
   i2c = board.I2C()  # Utilizes board's SCL and SDA pins
   oled = adafruit_ssd1306.SSD1306_I2C(WIDTH, HEIGHT, i2c, addr=0x3C)

   # Clear the OLED display
   oled.fill(0)
   oled.show()

   # Create a new image with 1-bit color for drawing
   image = Image.new("1", (oled.width, oled.height))

   # Obtain a drawing object to manipulate the image
   draw = ImageDraw.Draw(image)

   # Draw a filled white rectangle as the background
   draw.rectangle((0, 0, oled.width, oled.height), outline=255, fill=255)

   # Define border size for an inner rectangle
   BORDER = 5
   # Draw a smaller black rectangle inside the larger one
   draw.rectangle(
      (BORDER, BORDER, oled.width - BORDER - 1, oled.height - BORDER - 1),
      outline=0,
      fill=0,
   )

   # Load the default font for text
   font = ImageFont.load_default()

   def getfontsize(font, text):
      # Calculate the size of the text in pixels
      left, top, right, bottom = font.getbbox(text)
      return right - left, bottom - top

   # Define the text to be displayed
   text = "Hello World!"
   # Get the width and height of the text in pixels
   (font_width, font_height) = getfontsize(font, text)
   # Draw the text, centered on the display
   draw.text(
      (oled.width // 2 - font_width // 2, oled.height // 2 - font_height // 2),
      text,
      font=font,
      fill=255,
   )

   # Send the image to the OLED display
   oled.image(image)
   oled.show()

此Python脚本在SSD1306 OLED屏幕上创建"Hello World!"显示。执行时：

1. 脚本初始化与OLED显示屏的I2C通信。
2. 创建一个用于绘制的黑白图像缓冲区。
3. 绘制白色背景和黑色边框矩形。
4. 计算文本位置，使"Hello World!"在显示屏幕上居中。
5. 将最终图像渲染到OLED显示屏。
6. 显示屏保持文本显示，直到断电或更新。

----------------------------------------------

**理解代码**

1. **库导入**

   代码导入显示控制和图像操作所需的库。

   .. code-block:: python

      import board
      import digitalio
      from PIL import Image, ImageDraw, ImageFont
      import adafruit_ssd1306

2. **显示初始化**

   OLED显示屏以指定的尺寸和I2C地址进行初始化。

   .. code-block:: python

      WIDTH = 128
      HEIGHT = 64
      i2c = board.I2C()
      oled = adafruit_ssd1306.SSD1306_I2C(WIDTH, HEIGHT, i2c, addr=0x3C)

3. **显示清除**

   通过用零（黑色）填充并更新来清除显示屏。

   .. code-block:: python

      oled.fill(0)
      oled.show()

4. **图像缓冲区创建**

   创建一个用于绘制操作的1位色图像缓冲区。

   .. code-block:: python

      image = Image.new("1", (oled.width, oled.height))
      draw = ImageDraw.Draw(image)

5. **绘制操作**

   在图像缓冲区上绘制矩形和文本。

   .. code-block:: python

      # Background rectangle
      draw.rectangle((0, 0, oled.width, oled.height), outline=255, fill=255)
      # Inner bordered rectangle
      draw.rectangle((BORDER, BORDER, oled.width - BORDER - 1, oled.height - BORDER - 1), outline=0, fill=0)

6. **文本渲染**

   使用字体度量将文本在显示屏上居中。

   .. code-block:: python

      font = ImageFont.load_default()
      (font_width, font_height) = getfontsize(font, text)
      draw.text((oled.width // 2 - font_width // 2, oled.height // 2 - font_height // 2), text, font=font, fill=255)

7. **显示更新**

   最终图像被发送到OLED显示屏。

   .. code-block:: python

      oled.image(image)
      oled.show()

----------------------------------------------

**故障排除**

1. **显示无任何内容**

   - **原因**：I2C地址错误、接线问题或I2C未启用。
   - **解决方案**：验证I2C地址（尝试0x3C或0x3D），检查所有连接，并在Raspberry Pi配置中启用I2C（\ ``sudo raspi-config``\ ）。

2. **I2C通信错误**

   - **原因**：I2C总线问题或设备未检测到。
   - **解决方案**：使用\ ``i2cdetect -y 1``\ 命令检查设备是否被检测到。确保I2C总线上没有其他设备冲突。

3. **文本或图形显示不正确**

   - **原因**：颜色值或坐标问题。
   - **解决方案**：请记住，在1位色模式下0=黑色，255=白色。确认坐标在显示边界内（宽度0-127，高度0-63）。

4. **库导入错误**

   - **原因**：缺少依赖或安装不正确。
   - **解决方案**：确保所有所需库已安装在正确的环境中，如果出现权限问题，尝试使用\ ``sudo``\ 运行。

----------------------------------------------

**扩展思路**

1. **系统信息显示**

   创建实时系统监视器，显示CPU使用率、内存和温度：

   .. code-block:: python

      import psutil
      import time

      while True:
          # Clear and redraw
          draw.rectangle((0, 0, oled.width, oled.height), fill=0)

          # Get system info
          cpu_percent = psutil.cpu_percent()
          memory = psutil.virtual_memory()
          temp = psutil.sensors_temperatures()['cpu_thermal'][0].current

          # Display info
          draw.text((0, 0), f"CPU: {cpu_percent}%", font=font, fill=255)
          draw.text((0, 16), f"RAM: {memory.percent}%", font=font, fill=255)
          draw.text((0, 32), f"Temp: {temp}C", font=font, fill=255)

          oled.image(image)
          oled.show()
          time.sleep(2)

2. **滚动文本**

   为较长的消息创建滚动文本效果：

   .. code-block:: python

      text = "This is a scrolling text message!"
      text_width = getfontsize(font, text)[0]

      for x in range(oled.width, -text_width, -1):
          draw.rectangle((0, 0, oled.width, oled.height), fill=0)
          draw.text((x, oled.height//2), text, font=font, fill=255)
          oled.image(image)
          oled.show()
          time.sleep(0.05)

3. **动画图形**

   使用基本形状创建简单动画：

   .. code-block:: python

      for i in range(0, oled.width-10, 5):
          draw.rectangle((0, 0, oled.width, oled.height), fill=0)
          draw.rectangle((i, 20, i+10, 30), fill=255)  # Moving rectangle
          oled.image(image)
          oled.show()
          time.sleep(0.1)

4. **多页面**

   创建带按钮控制的多页面显示：

   .. code-block:: python

      pages = ["Page 1: Info", "Page 2: Stats", "Page 3: Graph"]
      current_page = 0

      # Use buttons to change pages
      def next_page():
          global current_page
          current_page = (current_page + 1) % len(pages)
          update_display()

5. **自定义字体**

   使用不同的字体以获得更好的视觉效果：

   .. code-block:: python

      # Load custom font (requires font file)
      try:
          custom_font = ImageFont.truetype("/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf", 12)
      except:
          custom_font = ImageFont.load_default()

6. **图形和图表**

   显示简单的条形图或图表：

   .. code-block:: python

      data = [10, 25, 45, 30, 60, 15]
      for i, value in enumerate(data):
          x = i * 20 + 5
          height = int(value / 100 * oled.height)
          draw.rectangle((x, oled.height-height, x+15, oled.height), fill=255)

----------------------------------------------

**总结**

本项目演示了如何通过Raspberry Pi上的I2C通信与SSD1306 OLED显示屏进行交互。OLED显示屏提供清晰、高对比度的视觉输出，适用于各种应用，包括系统监控、用户界面和信息显示。通过结合文本、形状和图形，你可以为项目创建信息丰富且视觉吸引力强的显示界面。这里学到的技能可扩展到创建复杂的用户界面、数据可视化和实时监控系统。
