.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_pir:

2.7 Sensor PIR
=================

**Introducción**

El sensor PIR (Infrarrojo Pasivo) es un dispositivo de detección de movimiento que percibe la radiación infrarroja emitida por humanos y animales. En este proyecto, utilizaremos el sensor PIR para detectar movimiento.



----------------------------------------------

**Qué Necesitarás**

A continuación se muestran los componentes necesarios para este proyecto:

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - INTRODUCCIÓN DEL COMPONENTE
        - ENLACE DE COMPRA

    *   - :ref:`cpn_pir`
        - |link_pir_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_fusion_hat`
        - \-
    *   - Raspberry Pi
        - \-


----------------------------------------------


**Diagrama de Circuito**

A continuación se muestra el diagrama esquemático de este proyecto:

.. image:: img/fzz/2.2.6_sch.png


----------------------------------------------


**Diagrama de Conexiones**

Construye el circuito como se muestra en el diagrama a continuación:

.. image:: img/fzz/2.2.6_bb.png
   :width: 80%
   :align: center

Asegúrate de que el LED RGB y el sensor PIR estén conectados a los pines GPIO correctos según el esquema.

.. note::

   Ajusta los dos potenciómetros en el módulo PIR para una sensibilidad y distancia de detección óptimas. Gira ambos en sentido antihorario hasta su ajuste máximo para obtener los mejores resultados.

.. image:: ../python/img/PIR_TTE.png
   :width: 200
   :align: center

----------------------------------------------

**Ejecutar el Ejemplo**


Todo el código de ejemplo usado en este tutorial está disponible en el directorio ``ai-lab-kit``.
Sigue estos pasos para ejecutar el ejemplo:


.. raw:: html

   <run></run>

.. code-block:: shell

   cd ~/ai-lab-kit/python/
   sudo python3 2.7_PIR.py

Este script de Python monitorea un sensor de movimiento PIR (Infrarrojo Pasivo) basado en la detección de movimiento. Cuando se ejecuta:

1. **Cuando se Detecta Movimiento**: Imprime "Detected Barrier!" en la consola.

2. **Cuando no se Detecta Movimiento**: Imprime "No Barrier" en la consola.

3. El programa se ejecuta indefinidamente, esperando eventos de movimiento. Se puede terminar presionando Ctrl+C.


----------------------------------------------


**Código**

A continuación se muestra el código de Python para este proyecto:


.. raw:: html

   <run></run>

.. code-block:: python

   #!/usr/bin/env python3
   from fusion_hat.pin import Pin, Mode, Pull
   from signal import pause  # Import pause function from signal module

   # Initialize a PIR Module object on GPIO pin 17
   pir = Pin(17, mode=Mode.IN, pull=Pull.DOWN)

   def detect():
      if pir.value() == 1:  # Check if the PIR Module is triggered
         print("Detected Barrier!")
      else:
         print("No Barrier")

   try:
      pir.when_activated = detect  # Set up an interrupt to detect changes in the pir sensor state
      pir.when_deactivated = detect

      # Run an event loop that waits for button events and keeps the script running
      print("CTRL + C to exit")
      pause()

   except KeyboardInterrupt:
      # Handle KeyboardInterrupt (Ctrl+C) to exit the loop gracefully
      pass


----------------------------------------------

**Comprender el Código**

1. **Importaciones:**

   .. code-block:: python

      from fusion_hat.pin import Pin, Mode, Pull
      from signal import pause  # Import pause function from signal module

   El script utiliza la librería ``fusion_hat`` para controlar el sensor PIR, y el módulo ``signal`` para manejar la terminación del programa.


2. **Inicialización:**

   .. code-block:: python

      pir = Pin(17, mode=Mode.IN, pull=Pull.DOWN)

   En esta línea, se inicializa un sensor PIR en el pin 17 con una resistencia pull-down interna habilitada.


3. **Bucle Principal:**

   .. code-block:: python

      def detect():
         if pir.value() == 1:  # Check if the PIR Module is triggered
            print("Detected Barrier!")
         else:
            print("No Barrier")

      try:
         pir.when_activated = detect  # Set up an interrupt to detect changes in the pir sensor state
         pir.when_deactivated = detect

         # Run an event loop that waits for button events and keeps the script running
         print("CTRL + C to exit")
         pause()

      except KeyboardInterrupt:
         # Handle KeyboardInterrupt (Ctrl+C) to exit the loop gracefully
         pass


   * Verifica continuamente si el sensor PIR detecta movimiento.
   * Si se detecta movimiento, se imprime el mensaje "Detected Barrier!".
   * Si no se detecta movimiento, se imprime el mensaje "No Barrier".


----------------------------------------------


**Solución de Problemas**


1. **No se Detecta Movimiento**:

   - **Causa**: El sensor PIR no está cableado o configurado correctamente.
   - **Solución**:

     - Asegúrate de que el sensor PIR esté conectado al pin GPIO 17, alimentación y tierra.
     - Ajusta los potenciómetros de sensibilidad y retardo del sensor si están disponibles.

----------------------------------------------

**Ideas para Extender**

1. **Retroalimentación de Audio**: Añade un zumbador que suene cuando se detecte movimiento:

   .. code-block:: python

      from fusion_hat import Buzzer
      buzzer = Buzzer(Pin(22))
      if pir.value() == 1:
         buzzer.on()
      else:
         buzzer.off()

2. **Registro de Datos**: Registra eventos de detección de movimiento con marcas de tiempo en un archivo para su análisis:

   .. code-block:: python

      with open("motion_log.txt", "a") as log_file:
         log_file.write(f"Motion detected at {time.time():.3f}\n")


----------------------------------------------


**Conclusión**

Este proyecto demuestra cómo usar un sensor PIR. Al comprender los conceptos básicos de la detección de movimiento, puedes expandir este proyecto hacia aplicaciones más avanzadas como sistemas de seguridad, iluminación automatizada o dispositivos interactivos. La versatilidad y simplicidad del sensor PIR lo convierten en un componente excelente para innumerables proyectos de bricolaje y profesionales.
