.. note::

    Hallo und willkommen in der SunFounder Raspberry Pi & Arduino & ESP32 Enthusiasten-Community auf Facebook! Tauchen Sie gemeinsam mit Gleichgesinnten tiefer in die Welt von Raspberry Pi, Arduino und ESP32 ein.

    **Why Join?**

    - **Expert Support**: Lösen Sie Nachkaufprobleme und technische Herausforderungen mit Unterstützung unserer Community und unseres Teams.
    - **Learn & Share**: Tauschen Sie Tipps und Tutorials aus, um Ihre Fähigkeiten zu erweitern.
    - **Exclusive Previews**: Erhalten Sie frühzeitigen Zugang zu neuen Produktankündigungen und Sneak Peeks.
    - **Special Discounts**: Profitieren Sie von exklusiven Rabatten auf unsere neuesten Produkte.
    - **Festive Promotions and Giveaways**: Nehmen Sie an saisonalen Aktionen und Verlosungen teil.

    👉 Bereit, mit uns zu entdecken und zu bauen? Klicken Sie auf [|link_sf_facebook|] und treten Sie noch heute bei!

.. _1.1.5_py:

1.1.5 4-stellige 7-Segment-Anzeige
======================================

**Introduction**

In diesem Projekt lernen Sie, eine 4-stellige 7-Segment-Anzeige so zu steuern, dass sie als Zähler dient. Der Zähler erhöht sich jede Sekunde um 1, und die Anzeige wird entsprechend aktualisiert. Das Projekt zeigt, wie sich GPIO-Pins zusammen mit einem 74HC595-Schieberegister nutzen lassen, um mehrere Stellen effizient anzusteuern.

----------------------------------------------

**What You’ll Need**

Für dieses Projekt benötigen Sie folgende Komponenten:

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - KOMPONENTE
        - KAUFLINK

    *   - :ref:`cpn_breadboard`
        - |link_breadboard_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_resistor`
        - |link_resistor_buy|
    *   - :ref:`cpn_4_digit`
        - \-
    *   - :ref:`cpn_74hc595`
        - |link_74hc595_buy|
    *   - Fusion HAT
        - 
    *   - Raspberry Pi Zero 2 W
        -


----------------------------------------------

**Circuit Diagram**

Das Schaltbild zeigt die Verbindung des 74HC595-Schieberegisters mit der 4-stelligen 7-Segment-Anzeige und den GPIO-Pins des Raspberry Pi. Das Schieberegister reduziert die Anzahl benötigter GPIO-Pins und ermöglicht so eine effiziente Ansteuerung der Anzeige.

.. image:: img/fzz/1.1.5_sch.png
   :width: 800
   :align: center


----------------------------------------------

**Wiring Diagram**

Gehen Sie wie folgt vor, um die Schaltung aufzubauen:

1. Platzieren Sie die 4-stellige 7-Segment-Anzeige und den 74HC595 auf dem Breadboard.
2. Verbinden Sie die Ausgänge des 74HC595 mit den Segmenten der Anzeige.
3. Verwenden Sie digitale Pins am Fusion HAT für Dateneingang (SER bzw. SDI), Schieberegister-Takt (SRCLK) und Latch-/Speicher-Takt (RCLK).
4. Fügen Sie Vorwiderstände hinzu, um den Strom für die Segmente zu begrenzen.

.. image:: img/fzz/1.1.5_bb.png
   :width: 800
   :align: center



----------------------------------------------

**Running the Example**


Der gesamte Beispielcode zu diesem Tutorial befindet sich im Verzeichnis ``ai-explorer-lab-kit``.  
Führen Sie die folgenden Schritte aus, um das Beispiel zu starten:


.. code-block:: shell
   
   cd ai-explorer-lab-kit/python/
   sudo python3 1.1.5_4-Digit_zero.py 


----------------------------------------------

**Writing the Code**

Der folgende Python-Code initialisiert die Anzeige, aktualisiert den Zähler und steuert die kontinuierliche Auffrischung der Anzeige:


.. raw:: html

   <run></run>

.. code-block:: python

   #!/usr/bin/env python3
   from fusion_hat import Pin
   import time
   import threading

   # Define GPIO pins for the 74HC595 shift register
   SDI = Pin(17,Pin.OUT)   # Serial Data Input
   RCLK = Pin(4,Pin.OUT)  # Register Clock
   SRCLK = Pin(27,Pin.OUT) # Shift Register Clock

   # Define GPIO pins for digit selection on the 7-segment display
   placePin = [Pin(pin,Pin.OUT) for pin in (23, 24, 25, 12)]

   # Define segment codes for numbers 0-9 for the 7-segment display
   number = (0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90)

   counter = 0  # Initialize counter for display
   timer1 = 0   # Initialize timer for counter increment

   def clearDisplay():
      """ Clear the 7-segment display. """
      for _ in range(8):
         SDI.high()
         SRCLK.high()
         SRCLK.low()
      RCLK.high()
      RCLK.low()

   def hc595_shift(data):
      """ Shift a byte of data to the 74HC595 shift register. """
      for i in range(8):
         SDI.value(0x80 & (data << i))  # Set SDI high/low based on data bit
         SRCLK.high()  # Pulse the Shift Register Clock
         SRCLK.low()
      RCLK.high()  # Latch data on the output by pulsing Register Clock
      RCLK.low()

   def pickDigit(digit):
      """ Select a digit for display on the 7-segment display. """
      for pin in placePin:
         pin.low()  # Turn off all digit selection pins
      placePin[digit].high()  # Turn on the selected digit

   def timer():
      """ Timer function to increment the counter every second. """
      global counter, timer1
      timer1 = threading.Timer(1.0, timer)  # Reset timer for next increment
      timer1.start()
      counter += 1  # Increment counter
      print("%d" % counter)  # Print current counter value

   def setup():
      """ Setup initial state and start the timer. """
      global timer1
      timer1 = threading.Timer(1.0, timer)  # Initialize and start the timer
      timer1.start()

   def loop():
      """ Main loop to update the 7-segment display with counter value. """
      global counter
      while True:
         for i in range(4):  # Loop through each digit
               clearDisplay()  # Clear display before setting new digit
               pickDigit(i)    # Select digit for display

               # Choose the digit of counter to display
               digit = (counter // (10 ** (3-i))) % 10

               hc595_shift(number[digit])  # Shift digit value to 74HC595
               time.sleep(0.001)  # Short delay for display stability

   def destroy():
      """ Cleanup GPIO resources and stop timer on exit. """
      global timer1
      timer1.cancel()  # Stop the timer
      for device in [SDI, RCLK, SRCLK] + placePin:
         device.close()  # Close GPIO devices

   try:
      setup()  # Initialize the setup
      while True:
         loop()  # Start the main loop
         
   except KeyboardInterrupt:
      # Handle script interruption (e.g., Ctrl+C)
      destroy()  # Cleanup resources on exit



Dieses Python-Skript steuert eine 4-stellige 7-Segment-Anzeige mit einem Raspberry Pi und einem 74HC595-Schieberegister. Bei der Ausführung gilt:

1. Die Anzeige zeigt einen hochzählenden Zähler, dessen Stellen im Sekundentakt aktualisiert werden.
2. Der aktuelle Zählerstand wird zu Debug-Zwecken in der Konsole ausgegeben.

Das Programm läuft unbegrenzt weiter und erhöht den Zähler, bis es vom Benutzer mit ``Ctrl+C`` beendet wird.


----------------------------------------------


**Understanding the Code**

1. **Initialize GPIO Pins**

   Weisen Sie die GPIO-Pins für das Schieberegister und die Stellenauswahl zu.

   .. code-block:: python

      # Define GPIO pins for the 74HC595 shift register
      SDI = Pin(17,Pin.OUT)   # Serial Data Input
      RCLK = Pin(4,Pin.OUT)  # Register Clock
      SRCLK = Pin(27,Pin.OUT) # Shift Register Clock

      # Define GPIO pins for digit selection on the 7-segment display
      placePin = [Pin(pin,Pin.OUT) for pin in (23, 24, 25, 12)]

2. **Segment Codes**

   Das Array ``number`` enthält die Bitmuster für die Ziffern 0–9.

   .. code-block:: python

      number = (0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90)

3. **Display Refresh**

   Die Funktion ``loop`` aktualisiert die Anzeige fortlaufend, indem sie jede Stelle selektiert und die passenden Daten sendet.

   .. code-block:: python

      def loop():
         global counter
         while True:
            for i in range(4):
               clearDisplay()
               pickDigit(i)
               digit = (counter // (10 ** (3-i))) % 10
               hc595_shift(number[digit])
               time.sleep(0.001)

4. **Counter Update**

   Die Funktion ``timer`` erhöht den Zähler einmal pro Sekunde.

   .. code-block:: python

      def timer():
         global counter, timer1
         timer1 = threading.Timer(1.0, timer)
         timer1.start()
         counter += 1
         print("%d" % counter)

5. **Cleanup**

   Die Funktion ``destroy`` stoppt den Timer und gibt beim Beenden die GPIO-Ressourcen frei.

   .. code-block:: python

      def destroy():
         """ Cleanup GPIO resources and stop timer on exit. """
         global timer1
         timer1.cancel()  # Stop the timer
         for device in [SDI, RCLK, SRCLK] + placePin:
            device.close()  # Close GPIO devices


----------------------------------------------

**Troubleshooting**

1. **Anzeige bleibt dunkel**  

   - **Ursache**: Falsche Verdrahtung oder fehlerhafte GPIO-Konfiguration.  
   - **Lösung**: Prüfen Sie die Verbindungen zum 74HC595 und stellen Sie sicher, dass die GPIO-Pins (17, 4, 27) den Variablen ``SDI``, ``RCLK`` und ``SRCLK`` entsprechen.

2. **Ziffern aktualisieren sich nicht korrekt**  

   - **Ursache**: Timing-Probleme oder fehlerhaftes Multiplexing.  
   - **Lösung**: Stellen Sie sicher, dass ``time.sleep(0.001)`` in ``loop()`` für eine stabile Anzeige ausreichend ist.

3. **Zähler setzt sich unerwartet zurück**  

   - **Ursache**: Der Thread-Timer ``timer1`` läuft nicht zuverlässig.  
   - **Lösung**: Vergewissern Sie sich, dass ``setup()`` vor der Hauptschleife ausgeführt und der Timer korrekt gestartet wird.

4. **KeyboardInterrupt funktioniert nicht**  

   - **Ursache**: ``destroy()`` wird beim Abbruch nicht ausgeführt.  
   - **Lösung**: Stellen Sie sicher, dass alle GPIO-Objekte in ``destroy()`` geschlossen werden und die Funktion im Block ``except KeyboardInterrupt`` aufgerufen wird.


----------------------------------------------

**Extendable Ideas**

1. **Angepasstes Zählerverhalten**  

   Lassen Sie abwärts zählen, bei einem Zielwert zurücksetzen oder spezielle Muster anzeigen.

2. **Mehrere Anzeigemodi**  

   Ergänzen Sie verschiedene Modi, z. B.:
   
   - Statische Textanzeige.
   - Wechsel zwischen Zähler und vordefinierter Meldung.

3. **Variable Zählgeschwindigkeit**  

   Ermöglichen Sie die dynamische Anpassung der Schrittgeschwindigkeit:

   .. code-block:: python

      speed = float(input("Enter counter speed in seconds: "))
      timer1 = threading.Timer(speed, timer)

4. **Echtzeituhr**  

   Ersetzen Sie den Zähler durch eine Uhr und zeigen Sie die aktuelle Zeit an:

   .. code-block:: python

      from datetime import datetime
      now = datetime.now()
      counter = now.hour * 100 + now.minute  # Display as HHMM


----------------------------------------------


**Conclusion**

Dieses Projekt zeigt, wie sich eine 4-stellige 7-Segment-Anzeige mithilfe eines 74HC595-Schieberegisters und GPIO-Programmierung ansteuern lässt. Die hier erlernten Techniken können Sie auf komplexere Anzeigen und interaktive Systeme übertragen.