.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_4_segment:

1.8 四位数码管
===============

**介绍**

在本项目中，我们将学习如何控制四位数码管来创建一个计数器。计数器每秒增加1，显示器相应更新。本项目突出了GPIO引脚和74HC595移位寄存器的使用，以高效管理多个数码管。

----------------------------------------------

**你需要什么**

要完成本项目，你将需要以下组件：

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - 组件
        - 购买链接

    *   - :ref:`cpn_breadboard`
        - |link_breadboard_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_resistor`
        - |link_resistor_buy|
    *   - :ref:`cpn_4_digit`
        - \-
    *   - :ref:`cpn_74hc595`
        - |link_74hc595_buy|
    *   - :ref:`cpn_fusion_hat`
        - \-
    *   - Raspberry Pi
        - \-


----------------------------------------------

**电路图**

电路图显示了如何将74HC595移位寄存器连接到四位数码管和Raspberry Pi的GPIO引脚。移位寄存器减少了所需的GPIO引脚数量，从而实现对显示器的有效控制。

.. image:: img/fzz/1.1.5_sch.png
   :width: 100%
   :align: center


----------------------------------------------

**接线图**

请按照以下步骤组装电路：

1. 将四位数码管和74HC595移位寄存器插入面包板。
2. 将74HC595的输出引脚连接到数码管的段引脚。
3. 使用Fusion HAT+的数字引脚作为数据输入（SER，也称为SDI）、移位寄存器时钟（SRCLK）和锁存寄存器时钟（RCLK）。
4. 添加电阻以限制数码管段的电流。

.. image:: img/fzz/1.1.5_bb.png
   :width: 80%
   :align: center



----------------------------------------------

**运行示例**


本教程中使用的所有示例代码都位于\ ``ai-lab-kit``\ 目录中。
请按照以下步骤运行示例：


.. raw:: html

   <run></run>

.. code-block:: shell

   cd ~/ai-lab-kit/python/
   sudo python3 1.8_4-Digit.py

运行脚本后，四位数码管显示一个每秒递增的计数器。当前计数器值也会打印到控制台。此过程持续进行，直到你按Ctrl + C停止程序。

----------------------------------------------

**代码**

以下Python代码初始化显示器、更新计数器并管理显示刷新循环：


.. raw:: html

   <run></run>

.. code-block:: python

   #!/usr/bin/env python3
   from fusion_hat.pin import Pin, Mode
   import time
   import threading

   # Define GPIO pins for the 74HC595 shift register
   SDI = Pin(17,mode=Mode.OUT)   # Serial Data Input
   RCLK = Pin(4,mode=Mode.OUT)  # Register Clock
   SRCLK = Pin(27,mode=Mode.OUT) # Shift Register Clock

   # Define GPIO pins for digit selection on the 7-segment display
   placePin = [Pin(pin,mode=Mode.OUT) for pin in (23, 24, 25, 12)]

   # Define segment codes for numbers 0-9 for the 7-segment display
   number = (0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90)

   counter = 0  # Initialize counter for display
   timer1 = 0   # Initialize timer for counter increment

   def clearDisplay():
      """ Clear the 7-segment display. """
      for _ in range(8):
         SDI.high()
         SRCLK.high()
         SRCLK.low()
      RCLK.high()
      RCLK.low()

   def hc595_shift(data):
      """ Shift a byte of data to the 74HC595 shift register. """
      for i in range(8):
         SDI.value(0x80 & (data << i))  # Set SDI high/low based on data bit
         SRCLK.high()  # Pulse the Shift Register Clock
         SRCLK.low()
      RCLK.high()  # Latch data on the output by pulsing Register Clock
      RCLK.low()

   def pickDigit(digit):
      """ Select a digit for display on the 7-segment display. """
      for pin in placePin:
         pin.low()  # Turn off all digit selection pins
      placePin[digit].high()  # Turn on the selected digit

   def timer():
      """ Timer function to increment the counter every second. """
      global counter, timer1
      timer1 = threading.Timer(1.0, timer)  # Reset timer for next increment
      timer1.start()
      counter += 1  # Increment counter
      print("%d" % counter)  # Print current counter value

   def setup():
      """ Setup initial state and start the timer. """
      global timer1
      timer1 = threading.Timer(1.0, timer)  # Initialize and start the timer
      timer1.start()

   def loop():
      """ Main loop to update the 7-segment display with counter value. """
      global counter
      while True:
         for i in range(4):  # Loop through each digit
               clearDisplay()  # Clear display before setting new digit
               pickDigit(i)    # Select digit for display

               # Choose the digit of counter to display
               digit = (counter // (10 ** (3-i))) % 10

               hc595_shift(number[digit])  # Shift digit value to 74HC595
               time.sleep(0.001)  # Short delay for display stability

   def destroy():
      """ Cleanup GPIO resources and stop timer on exit. """
      global timer1
      timer1.cancel()  # Stop the timer
      for device in [SDI, RCLK, SRCLK] + placePin:
         device.close()  # Close GPIO devices

   try:
      setup()  # Initialize the setup
      while True:
         loop()  # Start the main loop

   except KeyboardInterrupt:
      # Handle script interruption (e.g., Ctrl+C)
      destroy()  # Cleanup resources on exit




此Python脚本使用Raspberry Pi和74HC595移位寄存器控制四位数码管。执行时：

1. 显示器显示递增的计数器，数字每秒更新一次。
2. 计数器值打印到控制台以供调试。

程序无限运行，递增显示的计数器，直到用户按下\ ``Ctrl+C``\ 中断脚本。


----------------------------------------------


**理解代码**

1. **初始化GPIO引脚**

   为移位寄存器和位选分配GPIO引脚。

   .. code-block:: python

      # Define GPIO pins for the 74HC595 shift register
      SDI = Pin(17,Pin.OUT)   # Serial Data Input
      RCLK = Pin(4,Pin.OUT)  # Register Clock
      SRCLK = Pin(27,Pin.OUT) # Shift Register Clock

      # Define GPIO pins for digit selection on the 7-segment display
      placePin = [Pin(pin,mode=Mode.OUT) for pin in (23, 24, 25, 12)]

2. **段码**

   ``number``\ 数组定义了数字0-9的二进制表示。

   .. code-block:: python

      number = (0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90)

3. **刷新显示**

   ``loop``\ 函数通过选择每个位并发送相应数据来持续更新显示器。

   .. code-block:: python

      def loop():
         global counter
         while True:
            for i in range(4):
               clearDisplay()
               pickDigit(i)
               digit = (counter // (10 ** (3-i))) % 10
               hc595_shift(number[digit])
               time.sleep(0.001)

4. **计数器更新**

   ``timer``\ 函数每秒递增计数器。

   .. code-block:: python

      def timer():
         global counter, timer1
         timer1 = threading.Timer(1.0, timer)
         timer1.start()
         counter += 1
         print("%d" % counter)

5. **资源清理**

   ``destroy``\ 函数在程序退出时停止定时器并释放GPIO资源。

   .. code-block:: python

      def destroy():
         """ Cleanup GPIO resources and stop timer on exit. """
         global timer1
         timer1.cancel()  # Stop the timer
         for device in [SDI, RCLK, SRCLK] + placePin:
            device.close()  # Close GPIO devices


----------------------------------------------

**故障排除**

1. **显示器无显示**

   - **原因**：接线错误或GPIO引脚配置不正确。
   - **解决方案**：仔细检查与74HC595移位寄存器的连接，确保GPIO引脚（17、4、27）与定义的变量（\ ``SDI``\ 、\ ``RCLK``\ 、\ ``SRCLK``\ ）匹配。

2. **数字更新不正确**

   - **原因**：时序问题或复用不正确。
   - **解决方案**：确保\ ``loop()``\ 函数中的\ ``time.sleep(0.001)``\ 足以实现稳定的显示更新。

3. **计数器意外重置**

   - **原因**：\ ``timer1``\ 线程定时器可能未正常运行。
   - **解决方案**：确认在执行主\ ``loop()``\ 之前已执行\ ``setup()``\ 函数，且定时器已正确启动。

4. **KeyboardInterrupt不起作用**

   - **原因**：\ ``destroy()``\ 函数可能未在中断时正确执行。
   - **解决方案**：确保所有GPIO设备在\ ``destroy()``\ 中正确关闭，并且在\ ``except KeyboardInterrupt``\ 块中调用了该函数。


----------------------------------------------

**扩展思路**

1. **自定义计数器行为**

   修改计数器以实现倒计数、在特定值重置或显示特定图案。

2. **多模式显示**

   添加不同的显示模式，例如：

   - 静态消息显示。
   - 在计数器和预定义消息之间交替显示。

3. **可变计数速度**

   允许用户动态更改计数器的递增速度：

   .. code-block:: python

      speed = float(input("Enter counter speed in seconds: "))
      timer1 = threading.Timer(speed, timer)

4. **实时时钟**

   用实时时钟替换计数器以显示当前时间：

   .. code-block:: python

      from datetime import datetime
      now = datetime.now()
      counter = now.hour * 100 + now.minute  # Display as HHMM


----------------------------------------------


**总结**

本项目演示了如何使用74HC595移位寄存器和GPIO编程控制四位数码管。这里学到的技能可应用于更复杂的显示器和交互系统。
