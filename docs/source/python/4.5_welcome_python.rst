.. include:: /index.rst
   :start-after: start_hello_message
   :end-before: end_hello_message

.. _py_fun_welcome:


4.5 Bienvenue
=====================

**Introduction**

Avez-vous deja remarque les portes automatiques dans les magasins qui s’ouvrent avec une sonnerie d’accueil lorsqu’une personne s’approche ? Ce projet reproduit cette fonctionnalite a plus petite echelle. A l’aide d’un capteur PIR, d’un servomoteur, d’une LED et d’un buzzer, vous allez creer un systeme miniature de "porte automatique". Lorsqu’un mouvement est detecte, le systeme allume une LED, "ouvre" la porte a l’aide d’un servomoteur et joue une sonnerie d’accueil avec un buzzer.


----------------------------------------------


**Ce dont vous aurez besoin**

Voici les composants necessaires pour ce projet :

.. list-table::
    :widths: 30 20
    :header-rows: 1

    *   - PRESENTATION DU COMPOSANT
        - LIEN D’ACHAT

    *   - :ref:`cpn_breadboard`
        - |link_breadboard_buy|
    *   - :ref:`cpn_wires`
        - |link_wires_buy|
    *   - :ref:`cpn_resistor`
        - |link_resistor_buy|
    *   - :ref:`cpn_pir`
        - |link_pir_buy|
    *   - :ref:`cpn_servo`
        - |link_servo_buy|
    *   - :ref:`cpn_buzzer`
        - |link_passive_buzzer_buy|
    *   - :ref:`cpn_transistor`
        - |link_transistor_buy|
    *   - :ref:`cpn_fusion_hat`
        - \-
    *   - Raspberry Pi
        - \-




----------------------------------------------


**Schema de circuit**

Voici le schema de circuit pour ce projet :

.. image:: img/fzz/4.1.5_sch.png
   :width: 100%
   :align: center



----------------------------------------------


**Schema de cablage**

Suivez le schema de cablage pour assembler le circuit :


.. image:: img/fzz/4.1.5_bb.png
   :width: 100%
   :align: center


.. note::

   Reglez les potentiometres du capteur PIR pour une sensibilite et une portee de detection optimales. Tournez les deux potentiometres completement dans le sens antihoraire pour de meilleures performances.

.. image:: ../python/img/PIR_TTE.png
   :width: 300
   :align: center


----------------------------------------------

**Execution de l’exemple**


Tous les exemples de code utilises dans ce tutoriel sont disponibles dans le repertoire ``ai-lab-kit``.
Suivez ces etapes pour executer l’exemple :


.. raw:: html

   <run></run>

.. code-block:: shell

   cd ~/ai-lab-kit/python/
   sudo python3 4.5_Welcome.py

----------------------------------------------


**Code**

Below is the Python script for the project:



.. raw:: html

   <run></run>

.. code-block:: python

   from fusion_hat.pin import Pin, Mode, Pull
   from fusion_hat.servo import Servo 
   from fusion_hat.modules import Buzzer
   from fusion_hat.pwm import PWM
   import time

   # GPIO pin setup
   ledPin = Pin(17, Pin.OUT)

   # Initialize a PIR Module object on GPIO pin 17
   pirPin = Pin(22, mode=Mode.IN, pull=Pull.DOWN)

   buzPin = Buzzer(PWM('P0'))

   # Initialize servo with custom pulse widths
   servoPin = Servo('P4')

   # Musical tune for buzzer, with notes and durations
   tune = [('C#4', 0.2), ('D4', 0.2), (None, 0.2),
         ('Eb4', 0.2), ('E4', 0.2), (None, 0.6),
         ('F#4', 0.2), ('G4', 0.2), (None, 0.6),
         ('Eb4', 0.2), ('E4', 0.2), (None, 0.2),
         ('F#4', 0.2), ('G4', 0.2), (None, 0.2),
         ('C4', 0.2), ('B4', 0.2), (None, 0.2),
         ('F#4', 0.2), ('G4', 0.2), (None, 0.2),
         ('B4', 0.2), ('Bb4', 0.5), (None, 0.6),
         ('A4', 0.2), ('G4', 0.2), ('E4', 0.2),
         ('D4', 0.2), ('E4', 0.2)]

   def setAngle(angle):
      """
      Move the servo to a specified angle.
      :param angle: Angle in degrees (0-180).
      """
      servoPin.angle(angle)       # Set servo position
      time.sleep(0.001)           # Short delay for servo movement

   def doorbell():
      """
      Play a musical tune using the buzzer.
      """
      for note, duration in tune:
         buzPin.play(note,float(duration))       # Play the note
      buzPin.off()               # Stop buzzer after playing the tune

   def closedoor():
      # Turn off LED and move servo to close door
      ledPin.off()
      for i in range(180, -1, -1):
         setAngle(i)             # Move servo from 180 to 0 degrees
         time.sleep(0.001)       # Short delay for smooth movement
      time.sleep(1)               # Wait after closing door

   def opendoor():
      # Turn on LED, open door (move servo), play tune, close door
      ledPin.on()
      for i in range(0, 181):
         setAngle(i)             # Move servo from 0 to 180 degrees
         time.sleep(0.001)       # Short delay for smooth movement
      time.sleep(1)               # Wait before playing the tune
      doorbell()                  # Play the doorbell tune
      closedoor()                 # Close the door after the tune

   def loop():
      # Main loop to check for motion and operate door
      while True:
         if pirPin.value()==1:
               opendoor()               # Open door if motion detected
         time.sleep(0.1)              # Short delay in loop

   try:
      loop()
   except KeyboardInterrupt:
      # Clean up GPIO on user interrupt (e.g., Ctrl+C)
      buzPin.off()
      ledPin.off()

Ce script Python integre un capteur de mouvement PIR, un servomoteur, une LED et un buzzer pour creer un systeme d'accueil automatise. Lors de l'execution :

1. **Detection de mouvement** : Un capteur de mouvement PIR connecte a la broche GPIO 22 detecte le mouvement.

2. **Automatisation de la porte** : Lorsqu'un mouvement est detecte :

     - Le servomoteur (sur PWM 4) ouvre une porte en passant de 0° a 180°.
     - La LED (sur la broche GPIO 17) s'allume.
     - Une sonnerie d'accueil est jouee via le buzzer (sur PWM 0).
     - Le servomoteur ferme la porte en revenant de 180° a 0°.
     - La LED s'eteint.

3. **Surveillance continue** : Le systeme surveille en permanence les mouvements et declenche la sequence ci-dessus chaque fois qu'un mouvement est detecte.

4. **Sortie propre** : Sur ``Ctrl+C``, le buzzer et la LED sont eteints et le script se termine correctement.


----------------------------------------------

**Comprendre le code**

1. **Detection de mouvement :** Le capteur PIR detecte le mouvement et declenche le systeme.

2. **Controle du servomoteur :** Le servomoteur ouvre et ferme la porte en utilisant des angles de 0° a 180°.

3. **Melodie du buzzer :** Un air d'accueil est joue a l'aide du ``Buzzer``.

4. **Reinitialisation :** Apres la sonnerie, le systeme ferme la porte et eteint la LED, pret pour l'evenement suivant.



----------------------------------------------


**Depannage**

1. **Mouvement non detecte** :

   - **Cause :** Capteur PIR mal branche ou interference environnementale.
   - **Solution :**

     - Assurez-vous que le capteur PIR est connecte a la broche GPIO 22, a l'alimentation et a la masse.
     - Ajustez les potentiometres de sensibilite et de delai du capteur si disponibles.

2. **Le servo ne bouge pas** :

   - **Cause :** Configuration incorrecte du servomoteur ou probleme d'alimentation.
   - **Solution :**

     - Verifiez que le servomoteur est connecte a P4 et alimente correctement.

3. **La sonnerie ne joue pas** :

   - **Cause :** Mauvaise configuration du buzzer ou format de melodie incorrect.
   - **Solution :**

     - Assurez-vous que le buzzer est connecte a P0.
     - Verifiez que la liste ``tune`` contient des paires note/duree valides.

4. **La LED ne s'allume pas** :

   - **Cause :** Problemes de cablage de la LED ou de configuration GPIO.
   - **Solution :** Verifiez que la LED est connectee a la broche GPIO 17 avec une resistance appropriee.

----------------------------------------------


**Idees d'extension**

1. **Sonnerie reglable** : Ajoutez plus de melodies ou permettez a l'utilisateur de choisir differentes sonneries pour differents evenements.

2. **Fonctionnement horaire** : Desactivez le systeme a des heures specifiques (par exemple, la nuit) :

     .. code-block:: python

         from datetime import datetime
         if 8 <= datetime.now().hour < 22:  # Fonctionne seulement entre 8h et 22h
             opendoor()

3. **Journalisation des donnees** : Enregistrez les evenements de detection de mouvement et les horodatages dans un fichier pour analyse :

     .. code-block:: python

         with open("motion_log.txt", "a") as log_file:
             log_file.write(f"{time.strftime('%Y-%m-%d %H:%M:%S')} - Motion detected\n")

4. **Messages vocaux** : Remplacez la sonnerie du buzzer par des messages vocaux pre-enregistres utilisant un haut-parleur.

----------------------------------------------


**Conclusion**

Ce projet reproduit la fonctionnalite des portes automatiques de maniere ludique et educative. Il introduit des concepts comme la detection de mouvement, le controle de servomoteur et la generation sonore, ce qui en fait un excellent point d'entree pour les projets IoT et d'automatisation. Essayez de l'etendre en ajoutant des fonctionnalites comme les notifications a distance ou l'integration cloud pour une surveillance en temps reel.
